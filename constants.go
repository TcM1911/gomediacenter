package gomediacenter

const (
	// SessionKeyHeader is the html header used for the session token.
	SessionKeyHeader = "x-mediabrowser-token"
	// SessionAuthHeader is the html header used by the client to identify the it self.
	// it contains client version and other data.
	SessionAuthHeader = "x-emby-authorization"
)
